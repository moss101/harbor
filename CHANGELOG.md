# Changelog

Generated by `tools/generate_changelog.py` from commit subjects; one section per `harbor-v*` tag, newest first.

## Unreleased

### App

- name the missing core once instead of ten times (`1839a5f1b5`)
- package only the ABI that has a native core (`1d3a1a55b3`)
- let a running skill be cancelled from the sheet (`651e21fc4e`)
- say why a live-core wait gave up (`0578ab0201`)
- Fit Score refusal, memory-pressure perf proxy, release assembler fixes (`6ad9b620b5`)
- first-run catalog, fuzzing with one fix, release workflow (`da12d955c3`)
- diagnostics without telemetry (encrypted crash log, export bundle) (`69a70cd9a0`)
- safe-commit of approved proposals through FFI and the run sheet; proposal diff (`b020beb544`)
- skill run sheet over the FFI skill-run surface (`39d137e47e`)
- complete frontend overhaul for iOS, Android and desktop (`603b149fda`)

### CI

- the bundle's changelog copy was a silent no-op (`fda0badf4e`)
- a skipped suite is missing evidence, not a pass (`19abe5411a`)
- the macOS release artifact shipped without the native core (`3a9c7cad86`)
- give the android release assets names that say what they are (`b189a9c073`)
- say on the draft release that its evidence bundle is partial (`2fe73dee50`)
- partial release bundles on CI, computed release_declared (`43cdaf7de2`)
- first-run catalog, fuzzing with one fix, release workflow (`da12d955c3`)
- stale-seal detector, cargo audit + cargo deny gates, kill/restart closed (`3156f1bce8`)

### Chore

- reseal the dossier over the two new files (`f3615f0fb3`)

### Contracts

- harbor.graph/v1 schema + run-event step branches (`b72cb5132b`)

### Core

- formula content gate, spaced-path redaction, XML well-formedness probe; Windows test cfg; ring plan (`87922ed628`)
- first-run catalog, fuzzing with one fix, release workflow (`da12d955c3`)
- diagnostics without telemetry (encrypted crash log, export bundle) (`69a70cd9a0`)
- safe-commit of approved proposals through FFI and the run sheet; proposal diff (`b020beb544`)
- graph executor, tool registry, eval harness, four skill graphs (`9ca9f803ef`)

### Docs

- the closeout runbook said to flip release_declared by hand (`b17311f519`)
- run the ring go/no-go rule instead of reading it (`79b9e6152a`)
- production plan — where we stand after the engineering track (phases A–C done, D blocked on operator resources) (`1df79b8d68`)
- decisions 0005/0006, STATUS sessions 31–32, production plan (`aa9533d827`)
- close-out runbook consolidating the eight blocked gates (prerequisites, resume commands, post-close verification) (`cdb7a42e25`)
- external-prerequisite audit — absence of all eight gate resources verified on this machine (`27aab37940`)

### FFI

- an op whose body panics must still reach a terminal state (`79d0b420f0`)
- answer the close request; fail requests when the worker dies (`c5ec2da726`)
- first-run catalog, fuzzing with one fix, release workflow (`da12d955c3`)
- diagnostics without telemetry (encrypted crash log, export bundle) (`69a70cd9a0`)

### Inference

- cassette provider + structured output on the GGUF path (`e90bff797f`)

### Release

- ship only the architectures that have a native core (`884e9d0a91`)

### Skills

- five more graphs (nine runnable), review tools, live tier 13/18, GGUF long-prompt fix (`5174105776`)
- eval suites and replay cassettes for the four runnable graphs (`e1ebed6b87`)

### Tools

- read what the evidence says, not just that it exists (`eebbdd22cb`)
- a skipped suite is missing evidence, not a pass (`19abe5411a`)
- let the operator confirm GA's three human clauses by name (`437ec6a4ce`)
- run the ring go/no-go rule instead of reading it (`79b9e6152a`)
- partial release bundles on CI, computed release_declared (`43cdaf7de2`)
- Fit Score refusal, memory-pressure perf proxy, release assembler fixes (`6ad9b620b5`)

### UI

- complete frontend overhaul for iOS, Android and desktop (`603b149fda`)


## harbor-v1.0.0-rc2

### App

- hermetic test keystores; Security.framework in iOS device link; windows-core CI job; refreshed third-party notices (`6be4e64cc8`)
- background-isolate FFI worker, persistent app-support data root, full journey UI, no-op sweep; CI fmt/clippy/analyze/test gates (`3db10d7f1d`)

### CI

- real-network tier is qualification-machine-local (HF edge resets runner IPs; offline gates stay in CI) (`195711ff23`)

### Core

- tail-expression keystore selection (windows needless_return); plaintext inspection creates its evidence dir on fresh checkouts (`e65edfa871`)
- qwen chat qualification to the fixture-or-network tier (ignored); keystore selection without tail returns (windows clippy); dossier seal regenerated (`7b071cb357`)
- as_chunks in the legacy knowledge migration (newer-clippy gate); dossier seal regenerated in the same commit (code commits invalidate it) (`4cc5b87332`)
- as_chunks for vector unseal (newer-clippy gate); allow dead code on the non-Apple keychain stub (windows clippy) (`a32b4d9f8d`)
- OS keystore adapters, sealed knowledge DB, background ops with progress/cancel, unique run IDs, durable device identity (`8adece5032`)

### Tests

- exact 64-hex-char injected roots at every open site (hermetic, valid 32-byte keys) (`94ecfcbeb4`)

### Other

- STATUS: CI fully green on session-29 HEAD; network tier recorded as qualification-machine-local (`a9368d6f33`)
- seal the package manifest over git-tracked files; make test fixture/dylib paths checkout-relative (`e931997613`)
- derived reports regenerated at the session-29 final commit (`2e3c85af43`)
- harbor_native: openEx binding (injected device root); ffi test made hermetic (no keychain contact) (`4fff488f7a`)
- STATUS: session-29 snapshot — ten-item post-RC round complete (`e394dbf260`)


## harbor-v1.0.0-rc1

### Other

- RC evidence completion: Android §10 reduced suite, EN/AR store screenshots, integrity checks, network capture rebound at RC (`3b50001ed1`)
- STATUS: session-28 RC snapshot — harbor-v1.0.0-rc1 frozen, gate report bound, external blockers enumerated (`4d9488ea6b`)
- iOS production-device embedding + release-qualification packaging (`1415e79a4e`)
- STATUS: session commit list includes iOS live-core wiring (d323bde) (`a8447cb8d4`)
- iOS simulator: live native core — Embed Harbor Native Core build phase + verified launch (`d323bde25f`)
- STATUS header: session-27 commit list and final HEAD (`f733072fc3`)
- macOS bundle wiring verified live: dylib in Contents/Frameworks + codesign (`5468ca0c9b`)
- iOS native core builds for the simulator; record app-bundle native-library gap (`a31734a86b`)
- Session 27 close-out: STATUS final counts, manifest current at 10/10 gate PASS (`c060dad667`)
- Fix TODAY/NOW qualification: deterministic clock was silently not applied (`14367aeb31`)
- Evaluation corpus expansion: 464 cases (116 en + 116 ar + 232 mixed), corpus hash rebound (`5e5a6bd6bd`)
- STATUS: next dependency-ready task (eval corpus expansion); manifest current (`eb54c4a0a3`)
- Session 27 close: authoritative STATUS + checklist rewrite, final evidence (10/10 suites PASS at 7115d49) (`06b1b60e39`)
- Perf qualification runner + Windows/packaging scripts + Android native core live + device tiers (`7115d493ad`)
- Office matrix full coverage + network-capture qualification + plaintext-at-rest encryption & inspection (`43ed690fe6`)
- Regenerate derived dossier reports after session-26 file changes (dossier PASS) (`de09abca64`)
- Session 26: DOCX/PDF preview dispatch fix + Work Canvas rendering (`dbe14028cc`)
- Thermal baseline: 10-min sustained generation, no throttling (evidence) (`f54737f29c`)
- Regenerate manifest after final verification sweep (dossier PASS) (`db214f6822`)
- Bundle latest-per-object dedupe + tombstone propagation (11 protocol) (`7aa31d685c`)
- Session 24: PPTX chart embedding (DrawingML chart parts) (`bed7f2999c`)
- Session 23: XLSX chart creation + round-trip preservation (matrix row 14) (`a3ed286ef1`)
- Session 22: DOCX TableCellSet typed op with merge-aware addressing (`9b04c90ef0`)
- Gate evidence generator: commit-bound suite results (10/10 suites PASS) (`61d9d05816`)
- Session 21: snapshot bundle transport for re-enrolling devices (`2e988d2b55`)
- Remove debug test from tree (`cb7a2bce99`)
- DOCX table-cell addressing: merge-aware (row,col)->paragraph map (`6ed17079e8`)
- Session 20: formatting-preserving DOCX replacement, SBOM commit versioning (`61cca04fe8`)
- Session 19: sync snapshots, device expiry (90-day horizon), deletion watermark (`7308febc66`)
- Session 18: Office Feature Matrix conformance deepening (`f9f1271032`)
- Session 17: harbor_sync — E2EE sync protocol core (M4 optional) (`aade7b190e`)
- Session 16: PDF text extraction with page mapping; iOS simulator launch verified (`0064c201bd`)
- Exclude bulk model store from git (re-acquirable; hashes pinned in profiles) (`4480d9deb9`)
- Session 15: performance baselines measured on the qualified reference device (`01ffa0b840`)
- Session 14: catalog key ceremony + committed signature verification, macOS launch evidence (`df6316fe02`)
- Session 13: production model qualified and bound (Qwen2.5-1.5B-Instruct) (`c28ee47994`)
- Session 12: qualified reference device binding, release builds, composer runtime wiring, test tier split (`f47be06886`)
- Session 11: catalog trust persistence, HF gated-repo auth (M4), SBOM in CI (`b71f16b78c`)
- Session 10: signed catalog drives acquisition; streaming downloads (`f91892f290`)
- Session 9: in-app HF model acquisition through the Egress Broker (`78179c4128`)
- STATUS: session 8 handoff (accessibility gates, SBOM, migration docs) (`956cf8ccb6`)
- Extend package-copy ignores to build artifacts; regenerate manifest (dossier PASS) (`217014bc63`)
- Session 8: accessibility audit gates, CycloneDX SBOM, migration/rollback docs (`d931839896`)
- Session 7: catalog signing (epochs + rotation), WCAG contrast audit (`a62c273700`)
- Regenerate package manifest after session 6 (dossier PASS) (`af8309ec64`)
- Session 6: EN/AR l10n coverage gates, live Harbor Lens, release checklist sweep (`23c300d8ff`)
- Regenerate package manifest after session 5 (dossier PASS) (`bb9f7f88bf`)
- Session 5: full on-device RAG loop (retrieve -> augment -> generate) (`2edeaa0c3d`)
- Session 4: live Knowledge/RAG over a trained embedding model (`315aad6dc5`)
- Session 3: Harbor Skill v1 (21 families), pinned EN/AR eval corpus, harbor_domain (`3603bdde93`)
- Session 2: live FFI wiring, chat templates + embeddings, harbor_render, iOS+Android builds (`5459d0e662`)
- Harbor: M0 contract freeze + M1 runtime core, artifacts, formula qualification, GGUF inference, Flutter shell, FFI (`4f90f73539`)

