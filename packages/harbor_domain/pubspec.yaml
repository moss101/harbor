name: harbor_domain
description: "Harbor presentation/domain types shared by app and UI packages."
publish_to: none
version: 0.1.0
environment:
  sdk: ">=3.4.0 <4.0.0"

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
