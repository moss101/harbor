name: harbor_app
description: "Harbor — your AI. Your models. Your device. Your work."
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  harbor_ui:
    path: ../../packages/harbor_ui
  harbor_native:
    path: ../../packages/harbor_native
  harbor_domain:
    path: ../../packages/harbor_domain
  path_provider: ^2.1.0
  file_selector: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  generate: true
